{
    "ext": "ts tsx",
    "ignore": ["**/*.(test|spec).(ts|js)"],
    "watch": ["server/**/*.ts", "src/**/*.ts", "src/**/*.tsx"],
    "exec": "cross-env TARGET=client webpack && cross-env TARGET=server webpack && node dist/server/server.js"
}
