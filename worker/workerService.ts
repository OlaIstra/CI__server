export const workerService = {
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    runCommand: async jobData => {
        //const { id, repoName, buildCommand, commitHash } = jobData;
        //async RepositoryCommandsService.cloneRepo(repoName);
    },
};
